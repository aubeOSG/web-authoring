import type { ProjectsApiPreviewAsset } from '../projects.types';

export const previewAsset: ProjectsApiPreviewAsset = {
  name: '/projects/preview-asset',
  type: 'invoke',
  fn: (req, res) => {
    
  },
};

export default previewAsset;