import type { ProjectsApiUpload } from '../projects.types';

export const upload: ProjectsApiUpload = {
  name: '/projects/upload',
  type: 'invoke',
  fn: (req, res) => {
    
  },
};

export default upload;