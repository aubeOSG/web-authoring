import previewLink from "./link";
import imageFetch from "./image-fetch";
import imageUpload from "./image-upload";

export const api = {
  previewLink,
  imageFetch,
  imageUpload
};

export default api;